import CodeLine from "../components/CodeLine";

const Result = ({ input }) => {
    return (
        <div>
            <CodeLine text={input}></CodeLine>
        </div>
    );
};

export default Result;
